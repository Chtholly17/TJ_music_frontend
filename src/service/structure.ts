export type loginFormat = {
    username: string
    password: string
}

export type registerFormat = {
    username: string
    password: string
    verification_code: string
}

export type test = {
    test_string: string
}
